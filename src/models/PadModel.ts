export type PadModel = {
  padID: number
  opsScheduleID: number
  gisPadID: number
  padName: string
  batchDrill: boolean
  isScheduled: boolean
  activityCode: number
  isSplit: boolean
  temp: boolean
}